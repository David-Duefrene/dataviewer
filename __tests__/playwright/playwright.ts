import { test as playwright } from '@playwright/test'
export const {
	afterAll, afterEach, beforeAll, beforeEach, describe, expect, use,
} = playwright

export const it = playwright
export const should = playwright
export const test = playwright
export const contain = playwright

export default playwright

